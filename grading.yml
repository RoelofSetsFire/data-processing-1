grades:
    prog1:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            design: integer
            style: integer
        calculation: (scope * (3 * correctness + 2 * design + style) / 150.0 * 9 + 1).round(1)
        hide_calculated: true
    prog2:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            design: integer
            style: integer
        calculation: (scope * (3 * correctness + 2 * design + style) / 150.0 * 9 + 1).round(1)
        hide_calculated: true
    prog3:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            design: integer
            style: integer
        calculation: (scope * (3 * correctness + 2 * design + style) / 150.0 * 9 + 1).round(1)
        hide_calculated: true
    prog4:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            design: integer
            style: integer
        calculation: (scope * (3 * correctness + 2 * design + style) / 150.0 * 9 + 1).round(1)
        hide_calculated: true
    prog5:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            design: integer
            style: integer
        calculation: (scope * (3 * correctness + 2 * design + style) / 150.0 * 9 + 1).round(1)
        hide_calculated: true
    prog6:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            design: integer
            style: integer
        calculation: (scope * (3 * correctness + 2 * design + style) / 150.0 * 9 + 1).round(1)
        hide_calculated: true
    design1:
        type: float
    design2:
        type: float
    design3:
        type: float
    design4:
        type: float
    design5:
        type: float
    design6:
        type: float

calculation:
    normal:
        design: 35
        tech: 65

design:
    grades:
        design1: 1
        design2: 1
        design3: 1
        design4: 1
        design5: 1
        design6: 1

tech:
    grades:
        prog1: 1
        prog2: 1
        prog3: 1
        prog4: 1
        prog5: 1
        prog6: 1
