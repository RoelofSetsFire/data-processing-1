grades:
    prog1:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            design: integer
            style: integer
        calculation: (scope * (3 * correctness + 2 * design + style) / 150.0 * 9 + 1)
        hide_calculated: true
    prog2:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            design: integer
            style: integer
        calculation: (scope * (3 * correctness + 2 * design + style) / 150.0 * 9 + 1)
        hide_calculated: true
    prog3:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            design: integer
            style: integer
        calculation: (scope * (3 * correctness + 2 * design + style) / 150.0 * 9 + 1)
        hide_calculated: true
    prog4:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            design: integer
            style: integer
        calculation: (scope * (3 * correctness + 2 * design + style) / 150.0 * 9 + 1)
        hide_calculated: true
    prog5:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            design: integer
            style: integer
        calculation: (scope * (3 * correctness + 2 * design + style) / 150.0 * 9 + 1)
        hide_calculated: true
    prog6:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            design: integer
            style: integer
        calculation: (scope * (3 * correctness + 2 * design + style) / 150.0 * 9 + 1)
        hide_calculated: true
    prog7:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            design: integer
            style: integer
        calculation: (scope * (3 * correctness + 2 * design + style) / 150.0 * 9 + 1)
        hide_calculated: true    
    design6:
        type: float
    design7:
        type: float

calculation:
    normal:
        design: 30
        tech: 70

design:
    grades:
        design6: 1
        design7: 1
        
tech:
    grades:
        prog1: 1
        prog2: 1
        prog3: 1
        prog4: 1
        prog5: 1
        prog6: 1
        prog7: 1
